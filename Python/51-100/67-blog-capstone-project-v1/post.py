class Post:
    def __init__(self, id, title, subtitle, body):
        """Initialize a post object containing id, title, subtitle and body."""
        self.id = id
        self.title = title
        self.subtitle = subtitle
        self.body = body
